const { josa } = require("./src/josa");
const {
  hasJongSeong,
  getSyllable,
} = require("./src/syllable");
const { isHangle } = require("./src/isHangul");

module.exports = {
  josa,
  hasJongSeong,
  getSyllable,
  isHangle,
};