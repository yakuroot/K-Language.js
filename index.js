const { josa } = require("./src/josa");
const {
  hasJongSeong,
  getSyllable,
} = require("./src/syllable");
const { isHangul } = require("./src/isHangul");

module.exports = {
  josa,
  hasJongSeong,
  getSyllable,
  isHangul,
};